import 'package:flutter/material.dart';

class Learning extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Learning')),
      body: Center(
        child: Text('Learning'),
      ),
    );
    // return Container(child: Text('Outline'),);
  }
}